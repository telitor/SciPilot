VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_WS_URL=ws://localhost:8000/ws

# Supabase keys are intentionally absent: the browser talks to FastAPI only.
# SUPABASE_SERVICE_ROLE_KEY must never be placed in a VITE_* variable.
